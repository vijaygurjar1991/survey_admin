import { ConvertToUrlPipe } from './convert-to-url.pipe';

describe('ConvertToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
